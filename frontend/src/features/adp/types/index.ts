export type DraftPickRow = {
  rank?: number;
  name: string;
  position: string;
  adp: number;
  min: number;
  max: number;
  percentDrafted: number;
};

export type GetPicksResults = {
  results?: any;
};
